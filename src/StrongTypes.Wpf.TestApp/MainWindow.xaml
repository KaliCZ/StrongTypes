<Window x:Class="StrongTypes.Wpf.TestApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:StrongTypes.Wpf.TestApp"
        Title="StrongTypes WPF binding probe"
        Width="520" Height="320">
    <Window.DataContext>
        <local:PersonViewModel />
    </Window.DataContext>
    <Grid Margin="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Name (NonEmptyString)" VerticalAlignment="Center" />
        <TextBox   Grid.Row="0" Grid.Column="1" Margin="6"
                   Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnExceptions=True, NotifyOnValidationError=True}" />

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Email" VerticalAlignment="Center" />
        <TextBox   Grid.Row="1" Grid.Column="1" Margin="6"
                   Text="{Binding Email, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnExceptions=True, NotifyOnValidationError=True}" />

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Age (Positive&lt;int&gt;)" VerticalAlignment="Center" />
        <TextBox   Grid.Row="2" Grid.Column="1" Margin="6"
                   Text="{Binding Age, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnExceptions=True, NotifyOnValidationError=True}" />

        <TextBlock Grid.Row="3" Grid.ColumnSpan="2" Margin="6,12,6,6" TextWrapping="Wrap"
                   Text="Edit a field to see whether the strong-typed source is updated. Validation errors render the standard red border when WPF cannot convert the text back to the source type." />
    </Grid>
</Window>
